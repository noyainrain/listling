# ---

class Content:
    self.content_type # = text image video audio website

    self.text       # text image video audio website <- additional
    # real content (stream or link)
    self.url        #      image video audio website <- external-data
    self.media_type #      image video audio         <- file-format
    # represent the content:
    self.image      #      image video audio website <- representation
    self.summary    #                        website <- website / representation
